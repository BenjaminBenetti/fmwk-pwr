{
  "name": "fmwk-pwr",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server"
  ],
  "scripts": {
    "server": "bun run --cwd packages/server start",
    "test": "bun test --cwd packages/server && bun test --cwd packages/shared",
    "typecheck": "bun run --cwd packages/server typecheck && bun run --cwd packages/shared typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}